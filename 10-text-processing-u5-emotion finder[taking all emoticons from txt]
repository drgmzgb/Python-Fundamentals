# u5 emotion finder of  text processing - take out the emoticons from text
# input:
# There are so many emoticons nowadays :P. I have many ideas :O what input to place here :)
index = 0
text = input()
for el in text:
    if el == ":":
        next_el = text[index+1]
        print(f":{next_el}")
    index += 1
